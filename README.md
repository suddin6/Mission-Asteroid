This was a collaborative project developed using Python and Pygame completed in April 2025. 

I was responsible for the: 
**star function** (along with partner), **background function** (along with partner), **spaceship function**, **move_spaceship function**, **asteroid function**, **move_asteroid function**, **bonus_ asteroid function**, **move_bonus function**, **fake_asteroid function**, **move_fake function**, **game_over function** (along with partner), **reset_game function**, **play_button function** (along with partner), **pause_button function**, **pause function**, **resume_button function**, **resume function**, **restart_button function**, **restart function**, **flashReset function**, **statistics function**.

My partner was responsible for the:
**star function** (along with me), **background function** (along with me), **laser_shoot function**, **laser_update function**, **countdown function**, **draw_timer function**, **collision function**, **extra_time function**, **move_clock function**, **score function**, **game_over function** (along with me), **start_screen function**, **play_button function** (along with me).

The project was done with IDLE so I wrote all the **docstrings and end-of-line comments** throughout the code, **combined both of our codes** into one working program, and **handled the main function and global variables**.

My partner **designed all of the UI elements**.
